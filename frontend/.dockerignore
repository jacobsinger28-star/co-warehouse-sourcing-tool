node_modules
dist
.vercel
.git
.DS_Store
*.log
npm-debug.log*
# NOTE: public/data.real.json is intentionally NOT ignored — it must reach the
# image so the server can gate it. It is moved out of public/ during the build.
