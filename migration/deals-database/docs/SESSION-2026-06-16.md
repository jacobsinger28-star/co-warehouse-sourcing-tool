# deals-database — session log (2026-06-16)

> This is **this** project's session record. Not to be confused with the root-level
> `SESSION_HANDOFF_2026-06-16.md`, which is a cross-reference handoff for the *sibling*
> `../NashbilleSourcing/` project. For project orientation start with `README.md` +
> `docs/ARCHITECTURE.md`.

## TL;DR for the next agent

The project is still **design-stage v0** — no chatbot stood up, Stage 0 consolidation not
started (blocked on external people, see below). This session added the first **code** (a
Stage 0 data-quality validator) and put the project under **its own git repo with a private
remote**. Nothing about the deal data changed; there is still no real deal data, only the
example CSV.

## What this session did

1. **Built the Stage 0 index validator** — makes the "Quality bar" in
   `claude-project/deal-archive-spec.md` mechanical, so a thin/typo'd row can't silently
   poison the chatbot's answers.
   - `tools/validate_index.py` — stdlib-only (no venv). Errors on: header ≠ canonical schema,
     empty dedupe-critical columns (`deal_id`/`address`/`owner_entity`/`stage`/`outcome_status`),
     bad/duplicate `deal_id` slug, off-vocab `asset_type`/`stage`/`outcome_status`/`why_passed_tag`,
     LOI'd/under-contract/closed deal with no `source_docs`, non-numbers, non-ISO dates.
     Warns on: passed deal w/ no reason, `why_passed_tag` on a non-passed deal, cap_rate outside
     1–20%, offer > ask, reversed dates, non-US state, non-integer pipedrive_id. `--strict` makes
     warnings fail. Exit 0 clean / 1 errors / 2 unreadable.
   - `tools/test_validate.py` — **27 tests, all green** (`python3 tools/test_validate.py`).
   - `tools/testdata/invalid.csv` — deliberately broken fixture; doubles as a worked example.
   - `tools/README.md` — usage.
   - Wired into the workflow: `README.md` (tree + next-moves), `deal-archive-spec.md` (quality
     bar now says "run the linter"), `how-to-use.md` (validate before every upload + refresh).

2. **Put `deals-database/` under its own git repo + private remote.**
   - The parent `SimiCapital/` workspace repo deliberately **ignores** each sub-project (see its
     `.gitignore`), so this folder versions itself — same pattern as `NashbilleSourcing`.
   - One commit: `d14b89f` "Initialize deals-database repo: Stage 0 validator + design docs".
   - `.gitignore` keeps the real deal archive (index + LOI docs) **out** of version control —
     per `ARCHITECTURE.md` those live in the shared drive, not a laptop. Only the example CSV is tracked.
   - Private remote (created by Raz in the GitHub UI): `git@github.com:korteraz/simicap-deals-db.git`
     → https://github.com/korteraz/simicap-deals-db . Pushed and **verified**: local HEAD ==
     remote `main` == `d14b89f`, clean tree.

## Verified facts (checked, not assumed)

- `python3 tools/test_validate.py` → `27 passed, 0 failed`.
- `python3 tools/validate_index.py data/deal-index.example.csv` → exit 0 (clean).
- `python3 tools/validate_index.py tools/testdata/invalid.csv` → exit 1 (9 errors, 7 warnings).
- `git ls-remote origin refs/heads/main` == local HEAD `d14b89f`. Working tree clean.

## Environment gotchas (save the next agent time)

- **`gh` is not installed**, no `GH_TOKEN`/`GITHUB_TOKEN` in env, and the safety classifier
  **blocks** extracting the keychain token to call the GitHub API. Net: the agent **cannot create**
  a GitHub repo. Workflow that works: **Raz creates the empty private repo in the UI, agent pushes
  via SSH.**
- **Plain SSH (port 22) to GitHub times out** in the Claude Code Bash sandbox. Pushing works over
  `ssh.github.com:443`:
  ```bash
  GIT_SSH_COMMAND='ssh -o HostName=ssh.github.com -p 443' git push
  ```
  From Raz's own terminal on a normal network, plain `git push` works as-is. If port 22 is ever
  blocked elsewhere, add to `~/.ssh/config`: `Host github.com` / `HostName ssh.github.com` / `Port 443`.

## What's left

**🟢 Zero external input — a new agent can just build:**
- Draft the **PRD-call question list** for Aaron/Andrew (the actual unblock — see below).
- Add a `CLAUDE.md` orientation file (the sibling project has one; this one doesn't yet).
- Expand `data/deal-index.example.csv` into a richer fixture covering every controlled-vocab value.
- More validator checks if useful (e.g. `relationship_owner` against a known-teammate list).

**🔴 Blocks Stage 0 — only Raz/Aaron/Andrew can answer:**
- **Where does Simi's LOI history actually live?** (Pipedrive attachments? shared drive? inboxes?) → ask Aaron.
- **Pipedrive plan tier + API token** → Andrew. Needed to export deal records into `deal-index.csv`.
- Is `NashbilleSourcing` sourcing-stage data in scope for the archive, or only closed/LOI'd deals?
- PII handling policy for owner enrichment (internal-only, access-controlled).

Until those are answered, consolidation can't honestly start — the docs forbid fabricating deal data.

## First moves if continuing

1. Read `README.md`, `docs/ARCHITECTURE.md`, then `tools/README.md`. Project memory
   (`MEMORY.md` in the agent memory dir) already points here.
2. `git status` (should be clean), confirm `origin` → `simicap-deals-db`.
3. Pick from the 🟢 list — drafting the Aaron/Andrew question list is the highest-leverage
   zero-input move, since it shortens the path to the 🔴 unblock.
